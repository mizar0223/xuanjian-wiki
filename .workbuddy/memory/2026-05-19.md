# 2026-05-19

## 造物 scripts/ 目录整合

### 起因
评估 `资料库/造物/scripts/` 下 11 个 .py 脚本，发现：
- 全部硬编码旧路径 `/data/workspace/rq0rlzeg/...`（云开发机），在本地 Mac 一行都跑不通
- 部分目录映射也已过期（旧 `02-灵物-灵资 / 04-灵器 / 05-灵宝` 等 vs 当前 16 类型目录）
- A 簇 5 个脚本是同一件事的多版本迭代，覆盖度 70%+
- 还有 10 个 `link_build_diff_v*` 历史产物（~3 MB）

### 完成内容

**新增公共模块** `_paths.py`：
- 优先环境变量 `XJ_CAOWU_ROOT`，否则 `Path(__file__).resolve().parent.parent` 推导
- 导出 `CAOWU_ROOT / PAGES_DIR / PEOPLE_DIR / WIKI_ROOT / WORKBUDDY_DIR / DIR_NS` 等
- 16 类型目录映射统一在此（含 `13-符箓` 兼容、`11-符箓` 兼容）
- 多机协同安全：本地 Mac、CVM、云开发机均可直接运行，不需改路径

**保留并清理 3 个核心脚本**：
- `build_links.py` — 内链建设 v3（A/B/C 三阶段，--apply / --dry-run）
- `link_audit.py` — 新合并：scan + diff + apply 三子命令
  - `scan_red_links.py / generate_fix_diff.py / apply_a1_fix.py` 已并入
- `generate_topic_pages.py` — 体系专题页生成（顺手修了 `符箓`：`11-符箓` → `13-符箓`）

**归档 9 个脚本到 `archive/`**：
- 一次性任务 6 个：analyze_entities / scan_and_analyze / density_analyzer / run_tasks / run_final_tasks / generate_wiki_templates
- 已合并 3 个：scan_red_links / generate_fix_diff / apply_a1_fix
- 写了 `archive/README.md` 说明已知问题（路径硬编码、目录映射过期、数据依赖）

**清理 10 个历史产物**：删除 `link_build_diff{,_v2,_v3,_v4,_v5}.{json,txt}` 共约 3 MB

**写了 `scripts/README.md`**：用法速查、跨机器迁移指南、扩展指南。

### Smoke Test 通过
- `_paths.py`：路径全部正确解析；造物 16 子目录全部命中映射
- `link_audit.py scan`：识别 1311 处红链（造物实体 465 + 人物 829）
- `build_links.py --phase A --dry-run`：流程 OK，0 处变更（A 阶段历史已修完）
- `generate_topic_pages.py`：9 个专题页全部生成，含修复后的符箓

### 净效果
- **11 .py + 10 历史产物 → 4 .py + 1 公共模块 + archive/**
- 体积：scripts/ 主目录从 ~3.0 MB 降到 ~58 KB
- 多机协同：彻底零硬编码路径
- 复用：DIR_NS 等关键映射只在 `_paths.py` 一处维护，不再多脚本各自抄

## 关键约定（写入长期记忆）
- 造物 scripts/ 路径策略：优先 `XJ_CAOWU_ROOT` 环境变量，否则脚本自身相对路径推导
- 新增脚本必须 `from _paths import ...`，禁止硬编码 `/data/...` 或 `/Users/...`
- 临时输出（diff 快照等）放 `scripts/` 自身，但脚本要支持 `--output` 自定义到 `.workbuddy/`
