# 交接提示：法宝Wiki工程 · 后续工作

## 当前状态

375个Wiki页面已全部产出在 `pages/` 目录，按 `B{批次}-L{层级}-{频率说明}` 子目录分类。工程体系在 `.workbuddy/wide_window/`。

## 已完成

- 375页wiki（L1-L3全⭐深度，L4-L6机械底版）
- 宽窗索引（7.1MB，375 context文件）
- workstate.json驱动断点恢复
- batch_wiki_generator带3KB文件保护
- QA脚本 qa_check.py（道统白名单校验）
- workflow.md含4条实战教训

## 后续可做的

1. **上传Wiki**：`pages/*.wiki` 批量上传到MediaWiki
2. **精修L4-L6**：选20个有趣低频实体读context深度推理
3. **道统全量审计**：跑 `qa_check.py` 检查所有页面
4. **新批次生成**：用 `batch_wiki_generator.py` 再跑所有批次（3KB保护已开启）

## 注意事项

- Agent并行上限 **3个**，多用fresh agent+default模式
- 生成器有3KB文件保护，但建议手动时先备份
- `archive/` 有旧方案产出可参考
- `backup/` 有31页高质备份不可动
